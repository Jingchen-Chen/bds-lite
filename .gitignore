# Raw and processed datasets are NOT redistributed (third-party licenses).
# Rebuild them locally from raw sources via scripts/convert_*.py (see docs/data_access.md).
data/
outputs/

# Large binaries belong on Zenodo, not in git (see ZENODO_MANIFEST.csv).
*.pt
*.pth
*.ckpt
*.npy
*.npz

# Logs (excluded to avoid leaking absolute paths / machine names).
*.log

# Python / tooling caches.
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/

# OS / editor.
.DS_Store
*.swp
